import { ShiftSelectionContainerDirective } from './shift-selection-container.directive';

describe('ShiftSelectionContainerDirective', () => {
	it('should create an instance', () => {
		const directive = new ShiftSelectionContainerDirective();
		expect(directive).toBeTruthy();
	});
});
