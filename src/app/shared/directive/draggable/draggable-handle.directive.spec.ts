import { DraggableHandleDirective } from './draggable-handle.directive';

describe('DraggableHandleDirective', () => {
	it('should create an instance', () => {
		const directive = new DraggableHandleDirective();
		expect(directive).toBeTruthy();
	});
});
