import { SuccessiveItemDirective } from './successive-item.directive';

describe('SuccessiveItemDirective', () => {
	it('should create an instance', () => {
		const directive = new SuccessiveItemDirective();
		expect(directive).toBeTruthy();
	});
});
