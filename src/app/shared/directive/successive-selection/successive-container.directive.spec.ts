import { SuccessiveContainerDirective } from './successive-container.directive';

describe('SuccessiveContainerDirective', () => {
	it('should create an instance', () => {
		const directive = new SuccessiveContainerDirective();
		expect(directive).toBeTruthy();
	});
});
