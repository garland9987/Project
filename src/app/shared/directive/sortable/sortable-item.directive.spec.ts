import { SortableItemDirective } from './sortable-item.directive';

describe('SortableItemDirective', () => {
	it('should create an instance', () => {
		const directive = new SortableItemDirective();
		expect(directive).toBeTruthy();
	});
});
